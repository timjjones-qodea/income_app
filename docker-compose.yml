services:
  retirement-income:
    build: .
    container_name: retirement-income
    restart: unless-stopped
    environment:
      RIE_DATA_DIR: /app/data
    ports:
      - "${RIE_PORT:-8010}:8000"
    volumes:
      - ./data:/app/data

